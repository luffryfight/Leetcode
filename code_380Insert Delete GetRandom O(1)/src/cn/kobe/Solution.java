package cn.kobe;

/**
 * 作用：
 * 2020/10/31
 */
public class Solution {
    /*
    *
    * if(cache.get(val) == null) return false;
        int index = cache.get(val);
        int v = mark.get(mark.size() - 1);
        mark.set(index, v);
        mark.remove(mark.size() - 1);
        cache.put(v, index);
        cache.remove(val);

        return true;
        * */
}
